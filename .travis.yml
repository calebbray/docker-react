sudo: required
services:
  - docker
  
before_install:
  - docker build -t calebbray/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true calebbray/docker-react npm run test -- --coverage

language: generic
